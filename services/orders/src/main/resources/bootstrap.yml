spring:
  application:
    name: orders-service
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/