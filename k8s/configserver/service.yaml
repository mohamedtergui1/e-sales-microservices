apiVersion: v1
kind: Service
metadata:
  name: config-server
  labels:
    app: config-server
spec:
  selector:
    app: config-server
  ports:
    - protocol: TCP
      port: 8888  # Port exposed by the Config Server
      targetPort: 8888  # Port exposed by the container
  type: ClusterIP